import React from "react";
import "../styles/ExampleBox.css"; // Ensure you have a CSS file for ExampleBox

const ExampleBox = ({ text }) => <div className="example-box">{text}</div>;

export default ExampleBox;
